;
; IP Database configuration
;
; $Id$
;

; Database settings
[database]

; mysql example
provider = mysql
host = localhost
database = ipdb
username = ipuser
password = secret

; sqlite example
; provider = sqlite
; file = ipdb.db

prefix = ipdb_


; Session
[session]
expire = 10m


; Skin
[skin]
skin = default
